# Modo local (LocalStack) — copiar para .env e ajustar
AWS_ENDPOINT_URL=http://localhost:4566
AWS_DEFAULT_REGION=us-east-1
AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test

# Modo AWS Real — remover AWS_ENDPOINT_URL e usar credenciais reais
# AWS_DEFAULT_REGION=us-east-1
# (usar aws configure ou variáveis de ambiente com credenciais reais)
